# Backend environment
# For docker-compose, copy to .env and fill the secrets
JWT_ACCESS_SECRET=change-me-access
JWT_REFRESH_SECRET=change-me-refresh
# If using external Mongo, override MONGO_URI here (otherwise compose uses local mongo service)
# MONGO_URI=mongodb://username:password@host:27017/almasdar?authSource=admin

# CORS: comma-separated list of allowed origins (e.g., your frontend URL)
CORS_ORIGIN=http://localhost,http://localhost:80,https://your-domain.com
